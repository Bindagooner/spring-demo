<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="neospider.mngr.bt.persistence.repositories.MyBatisBookRepository" >
    <!--<cache />-->
    <resultMap id="baseResultMap" type="neospider.mngr.bt.persistence.entities.BookEntity" >
        <id column="id" property="id" jdbcType="VARCHAR" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        <result column="author" property="author" jdbcType="VARCHAR" />
    </resultMap>

    <select id="findById" resultMap="baseResultMap" parameterType="string">
        SELECT * FROM book WHERE id = #{id}
    </select>

    <select id="listAll" resultMap="baseResultMap">
        SELECT * FROM book
    </select>

    <insert id="insert" parameterType="neospider.mngr.bt.persistence.entities.BookEntity">
        INSERT INTO book(id, name, author) VALUES (#{id}, #{name}, #{author})
    </insert>
</mapper>
